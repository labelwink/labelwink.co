'use client'

// Analytics page is now fully client-side — data fetched in AnalyticsClient.tsx
import AnalyticsClient from './AnalyticsClient'

export default function AnalyticsPage() {
  return <AnalyticsClient />
}
