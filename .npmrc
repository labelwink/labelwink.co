# Required because eslint-config-next@16.x requires eslint@>=9
# but we pin eslint@^8 for compatibility with existing config.
# This flag is equivalent to `npm install --legacy-peer-deps`.
legacy-peer-deps=true
